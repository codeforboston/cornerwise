define(["backbone"],
       function(B) {
           return B.Model.extend({
               initialize: function() {

               }
           }, {
               modelName: "parcel"
           });
       });
