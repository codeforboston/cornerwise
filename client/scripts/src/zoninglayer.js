define(["leaflet"],
       function(L) {
           var ZoningLayer = L.Class.extend({
               initialize: function(permits) {

               }
           });

           return ZoningLayer;
       });
